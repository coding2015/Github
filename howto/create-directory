how to create a directory or path in repository:
1.create directory in github:
    in the file input: enter a path separated by '/', the github will create directory automatically by '/'
    reference link:
        http://webapps.stackexchange.com/questions/36411/create-a-folder-in-github-via-the-web-interfacea

2.push directory from local:
    add-commit-push a new none-empty direcory
        note: the directory should be none-emptyf
