前提：本地和github已存在pubkey关系（见Github/initialize）

1、在github新建一个repository #注意这一步不要初始化，（不要勾选‘Initialize this repository with a README ‘）
                              #初始化需在本地进行
2、进入本地项目目录
   $ git init  
   $ git add *
   $ git commit -m 'xxxxx'
   $ git remote add origin git@github.com:coding2015/xxx.git
   $ git push -u origin master
